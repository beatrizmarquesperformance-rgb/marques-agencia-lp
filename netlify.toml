[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  # Prisma client is generated in the build command; no DB needed to build.
  PRISMA_SKIP_POSTINSTALL_GENERATE = "false"

[[plugins]]
  package = "@netlify/plugin-nextjs"
